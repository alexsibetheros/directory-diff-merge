#ifndef LIST
#define LIST

////////////////////////////////////////////////////////////////////////////////////////////////////////////
struct node
{
	char* path;/*Path is used for fringe, it holds the common directories to allow program to search each one for differences or similarities*/
	int inode; /*Inode is used in inode_list, with which we check if an inode has already been written to dirC, thus linking is neccessary and not writing again*/
	struct node *link;
};
////////////////////////////////////////////////////////////////////////////////////////////////////////////
void printlist ( struct node *p );
////////////////////////////////////////////////////////////////////////////////////////////////////////////
struct node *insert(struct node *p, char* path,int inode);
////////////////////////////////////////////////////////////////////////////////////////////////////////////
char* pop_list(struct node **p);
////////////////////////////////////////////////////////////////////////////////////////////////////////////
char* search_inode(struct node* p, int inode_num,int* result); 
////////////////////////////////////////////////////////////////////////////////////////////////////////////
void delete_list(struct node *p);
////////////////////////////////////////////////////////////////////////////////////////////////////////////














#endif /*LIST*/
